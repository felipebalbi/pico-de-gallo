[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "pyco-de-gallo"
version = "0.4.2"
description = "Rust-powered Pico de Gallo bindings"
readme = "README.md"
requires-python = ">=3.8"
